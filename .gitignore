node_modules/
dist/
.DS_Store
.claude/

# pages() プラグイン（vite.config.ts）が episodes.json から生成する本文/タイトルページ。
# 雛形は templates/ に、生成物はここに出る。追跡しない（先頭スラッシュで root 直下のみ＝public/contents は対象外）。
/contents/

# vitest coverage レポート（`npm run test:coverage` で生成される HTML 出力）。ローカル可視化用で追跡しない。
coverage/

# Playwright スモーク（`npm run test:e2e`）の出力。ローカル既定・追跡しない。
playwright-report/
test-results/
.playwright/